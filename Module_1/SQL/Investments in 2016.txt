SELECT ROUND(SUM(tiv_2016),2) as tiv_2016 FROM Insurance WHERE concat(lat,"-",lon) in (SELECT concat(lat,"-",lon) as latlon FROM Insurance GROUP BY latlon HAVING COUNT(*)=1) AND tiv_2015 IN (SELECT tiv_2015 FROM Insurance GROUP BY tiv_2015 HAVING COUNT(*)>1);
